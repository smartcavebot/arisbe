# Byte-compiled / cache
__pycache__/
*.pyc

# Test cache
.pytest_cache/

# OS files
.DS_Store

# Node.js
node_modules/

# Generated artifacts
*.cgif
clean_egi_output.json
corpus_conversion_results.json
corpus_demo_results.json

debug_output.txt

# Local docs/scripts to ignore
INSTALL_QT_GUI.md

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be added to the global gitignore or merged into this project gitignore.  For a PyCharm
#  project, it is recommended to comment the following line:
#.idea/

# Obsolete documentation (archived to prevent context contamination)
docs/archived/
!docs/archived/obsolete_egdf/EGDF_OBSOLETE_WARNING.md

# Graphify knowledge-graph output (regenerated on demand)
graphify-out/

# Claude Code local state and one-shot authorization markers
.claude/
.core_modification_authorized

# Ergasterion workshop scratch (regime-1 drafts; not part of the corpus)
/scratch/

# Liveness / desuetude log — local forward-facing usage state (manifest floor #7);
# churns on every consultation, so it rides beside the corpus, not inside it.
tomos/.liveness.json

# Coherence reminder counter — local tooling state (timestamp + count); churns
# on every commit, so it rides beside the repo, not inside it (cf .liveness.json).
.coherence_reminder_state

# Quarto book/help generated outputs
docs/_book/
docs/.quarto/

# Live-run side store (checkpoint UoDs + state + recorded polls are run artifacts,
# never the corpus; the run LOG is tracked)
runs/*/checkpoints/
runs/*/state.json
runs/*/frontier.json
runs/*/polls.jsonl
runs/*/items.jsonl
runs/*/weather_state.json
runs/*/asks.jsonl
runs/*/STOP

# Live-run output directories (RUN 13, the vault cycle, and every numbered
# run dir after it) — custody boundary: everything a live driver writes
# against a real vault/source (UoDs/chains/journals/side-stores) is
# model-derived from private material and never enters git. Whole-branch
# review (2026-07-19, IMPORTANT 2): the literal `runs/run13/` rule only
# covered one hardcoded number, but `--runs-dir` is a free argument and run
# directories get renumbered across sessions (RUN 14, 15, ... already
# routine) — a future run one `git add -A` away from committing verbatim
# author prose. Generic glob instead: ANY `runs/run<digits...>/` directory
# is ignored wholesale (not just checkpoint subpaths, as above).
#
# Re-review (2026-07-20, NEW-3): the prior comment here claimed the
# top-level aggregate logs (`runs/RUN_*_LOG.md`, `runs/OPERATIONS.md`,
# `runs/EXAMINATION_IV_BRIEFS/`) were spared because of their UPPERCASE
# `RUN_` — FALSE on this repo: `core.ignorecase=true` (the default on a
# case-insensitive filesystem, e.g. macOS/APFS), so git casefolds the
# glob before matching (verified: `runs/run*/` DOES match a hand-made
# `runs/RUN_TEST_DIR/` directory here). The reasoning that actually holds:
# the trailing slash makes this glob directory-only, so it can never match
# a FILE regardless of case (`RUN_12_LOG.md`, `OPERATIONS.md`); and the
# pattern below is now tightened to require digits right after `run`
# (case-insensitively), so `EXAMINATION_IV_BRIEFS/` (doesn't start with
# "run" at all) AND a future `RUN_14_BRIEFS/`-shaped DIRECTORY (starts
# with "run" case-folded, but not followed by a digit) both stay
# unmatched on every platform, not just case-sensitive ones. Already-
# tracked files under earlier `runs/runN/` directories (e.g.
# `runs/run3/filmstrip/*.svg`, `runs/run10/console.txt`) are unaffected —
# .gitignore only screens untracked paths, never untracks what git
# already knows about.
runs/run[0-9]*/

# West-in-kytē E2 sweep output — same custody boundary as above (a synthetic
# vault, but the driver's own working directory is not corpus content). No
# trailing slash (final review, Minor 1): a slash-terminated glob is
# directory-only and would silently miss a console redirect like
# `> runs/west_e2_console.txt` — the exact footgun that left
# runs/run13_console.txt untracked-and-unignored.
runs/west_e2*
# ...but the tracked aggregate run log is spared (case-insensitive filesystems
# match `west_e2*` against the UPPERCASE name, exactly as the RUN_*_LOG.md logs
# are spared above — the log is committed, the console/data outputs are not).
!runs/WEST_E2_LOG.md
# ...and the E2b run log too (same case-insensitive-glob reasoning).
!runs/WEST_E2B_LOG.md

# E3 (same pattern: outputs ignored, the tracked run log spared).
runs/west_e3*
!runs/WEST_E3_LOG.md

# E3b (same pattern: outputs ignored, the tracked run log spared).
runs/west_e3b*
!runs/WEST_E3B_LOG.md
runs/west_e3c*
.superpowers/
